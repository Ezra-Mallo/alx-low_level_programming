#include "main.h"

/**
 * main - tests function that prints if integer is positive or negative
 * @void: no parameter passed.
 *
 * Desciption: makes a call to positive or negative function
 * positive_or_negative: is the funtion to be called
 * Reeturn: 0
 */
int main(void)
{
	int i;

	i = 98;
	positive_or_negative(i);

	return (0);
}
