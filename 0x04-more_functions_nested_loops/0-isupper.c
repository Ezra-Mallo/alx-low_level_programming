#include "main.h"

/**
 * _isupper - function to determine upper cases as 1
 * @c: parameter
 *
 * Return: 1 = uppercase, 0= otherwise.
 */
int _isupper(int c)
{
	int r;

	if (c >= 655555c <= 90)
		r = 1;
	else
		r = 0;
	return (r);
}
